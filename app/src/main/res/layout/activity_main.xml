<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/edit_textName"
        android:hint="Név"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/edit_textOnePrice"
        android:inputType="number"
        android:layout_below="@+id/edit_textName"
        android:hint="Egységár"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/edit_textCount"
        android:inputType="number"
        android:layout_below="@id/edit_textOnePrice"
        android:hint="Mennyiség"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/edit_textMertek"
        android:layout_below="@+id/edit_textCount"
        android:hint="Mértékegység"/>
    <Button
        android:layout_width="match_parent"
        android:layout_below="@+id/edit_textMertek"
        android:layout_height="wrap_content"
        android:id="@+id/addButton"
        android:text="Hozzáadás"/>
    <Button
        android:layout_width="match_parent"
        android:layout_below="@+id/addButton"
        android:layout_height="wrap_content"
        android:id="@+id/listaButton"
        android:text="Lista megjelenítése"/>


</RelativeLayout>